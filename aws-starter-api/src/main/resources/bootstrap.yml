spring:
  application:
    name: aws-starter-api
  config:
    import: configserver:http://13.52.157.48:8888
  cloud:
    config:
      uri: http://13.52.157.48:8888
      fail-fast: false
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 6

logging:
  level:
    root: INFO
    org.testcontainers: DEBUG
    org.springframework.test: DEBUG
    org.springframework.boot.test: DEBUG
    com.example.awsstarterapi: DEBUG

spring:
  data:
    mongodb:
      database: test_db

server:
  port: 0  # Random port for testing

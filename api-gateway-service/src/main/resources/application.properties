# This file is kept as a fallback but configuration is now managed by Config Server
# See bootstrap.yml for the minimal configuration needed to connect to Config Server

# Actuator endpoints for local development and health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

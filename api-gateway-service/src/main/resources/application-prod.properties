# This file is kept as a minimal fallback for production profile
# Configuration is now managed by Config Server (api-gateway-service-prod.yml)

# Actuator endpoints for production health checks
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never

# Disable development-specific features
spring.devtools.restart.enabled=false
